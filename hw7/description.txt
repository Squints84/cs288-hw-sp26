the way i approached the problem was finding the easiest functions to implement and working my way up. first i did the swap function, which is stuff that we know, just making sure to properly use the struct in the skeleton to access the right variables. then the move functions is just using swap and then plus one and minus one on the rows and columns. next was making expand. first i had to check if the move was legal, so checked if the location of the 0 was within the bounds. then malloc'd a new child node, copied the blank location, and then moved the child, where i stored in a succ_nodes array. next was update_fgh. i made a new function to calculate the Manhattan distance because it felt too cluttered to calculate it in the function. first was g, which is the path cost. h was the Manhattan distance and f is just the sum of those. filter was next. first sanity check to make sure the successor node wasn't null and then traverse the list using nodes_same to add the right nodes to the list. then  merge to open about handling the different possible cases for each f. first case is where the list is empty, where the new node just becomes the head of the list. then the second case where the f of the new node has a lower f than the head, where we just insert at the front. and the third case where we just keep going until we find a node with a higher f and insert it there. then adding some logic to make sure you don't invalid number of variables and that's it. 