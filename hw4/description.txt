1) open files with fread command in binary mode bc bin files, and then read the first line for matrix dimensions. checked to make sure dimensions were equal and not over 100 and then dynamically allocated matrices. calloc'd the result matrix so the  values would be initalized as 0. then looped thru result matrix adding dot product at each index. then write matrix to another
bin file, and free and close everything.

2) open files with fscanf, and then read the number of rows and non-zero values for each sparse matrix. checked to make sure row counts were equal and then dynamically allocated the CSR arrays (values, column indices, row start) for both matrices and the result. did a first pass over each row with two pointers to count how many non-zero entries the sum would have, then a second pass to actually merge and add into the result CSR arrays while keeping columns sorted. finally wrote the result CSR back out to a txt file in the same format and then free and close everything.